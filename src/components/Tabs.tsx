import React from 'react';

/**
 * Компонент вкладок.
 */
export const Tabs: React.FC = () => {
  return (
    <div>
      <button>Сейчас в СМИ</button>
      <button>В Германии</button>
      <button>Рекомендуем</button>
    </div>
  );
};
